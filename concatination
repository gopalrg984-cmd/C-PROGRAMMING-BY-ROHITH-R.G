#include <stdio.h>
#include <string.h>

int main() {
    char str1[30] = "VTU";     // first string
    char str2[20] = "College"; // second string

    strcat(str1, str2);        // join str2 to str1

    printf("Concatenated String: %s\n", str1);

    return 0;
}
