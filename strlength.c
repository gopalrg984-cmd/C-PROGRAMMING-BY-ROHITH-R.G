#include <stdio.h>
#include<string.h>
    int main(){
        char name[10];
        printf("enter the name\n");
        scanf("%s",name);
        int length=strlen(name);
        printf("the length is: %d\n",length);
    }
 
